# App
PORT=8000

# Database
# For local `go run` use localhost; docker-compose overrides DB_HOST to "db"
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=go_crud
DB_SSL_MODE=disable
DB_TIMEZONE=Asia/Kolkata
